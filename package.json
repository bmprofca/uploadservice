{
  "name": "upload-service",
  "version": "1.0.0",
  "description": "File upload API — public URLs and file metadata",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "upload",
    "express",
    "file-host"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "image-size": "^2.0.2",
    "multer": "^2.1.1"
  }
}
